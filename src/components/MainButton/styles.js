import { StyleSheet } from "react-native";

const style = StyleSheet.create({
	bigBubble: {
		justifyContent: 'center',
		alignItems: 'center',
		backgroundColor: '#7DA5D1',
		height: 100,
		width: 100,
		borderRadius: 100 / 2,
		top: -30,
	},
});

export default style;